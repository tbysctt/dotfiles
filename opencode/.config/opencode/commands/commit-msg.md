---
description: Propose a commit message from the current diff (no commit)
agent: plan
---
Propose a concise commit message for the current changes.

Inspect the staged and unstaged diff. Prefer a short why-focused subject (and body only if needed). Offer 1–2 alternatives if the change spans multiple concerns.

Do not run `git add`, `git commit`, or `git push`. I will commit myself.
