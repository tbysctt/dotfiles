---
description: Run project tests and fix failures
agent: build
---
Detect how this project runs tests, run them, and fix any failures.

Prefer the project's documented test command (README, package scripts, Makefile, etc.). After changes, re-run the relevant tests and report what failed, what you fixed, and what still needs attention. Do not commit or push.
